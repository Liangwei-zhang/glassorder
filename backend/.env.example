PORT=8782
DB_PATH=./glass.db
UPLOADS_DIR=./uploads
JWT_SECRET=change-this-in-production
SEED_DEMO_USERS=0
LABEL_COMPANY_NAME=SUNSHINE TEMPERED GLASS

# Optional SMTP. If not configured, pickup still generates PDFs and skips email.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
